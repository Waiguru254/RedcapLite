% Generated by roxygen2: do not edit by hand
\name{tabmult}
\alias{tabmult}
\title{Tabulate Multiple-Choice (mchoice) Variables}
\usage{
tabmult(formula, data, percent_by = "column", show.na = TRUE)
}
\arguments{
\item{formula}{A formula of the form \code{column ~ by} or \code{column ~ NULL}.}

\item{data}{A data frame containing the variables in the formula.}

\item{percent_by}{Character string specifying how to calculate percentages:
\code{"column"} (default) or \code{"row"}. Only used for two-way tables.}

\item{show.na}{Logical value indicating whether to include missing values.}
}
\value{
A data frame with counts, percentages, labels, and p-values. The result has
class \code{tabmult} and prints a formatted table in the console.
}
\description{
Create one-way or two-way tabulations for \code{mchoice} variables, including
counts, percentages, value labels, variable labels, and per-level p-values.
}
\examples{
\dontrun{
# One-way
tabmult(symptoms ~ NULL, data = export_data)

# Two-way
tabmult(symptoms ~ gender, data = export_data)
}
}
