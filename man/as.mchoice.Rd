% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mchoice.R
\name{as.mchoice}
\alias{as.mchoice}
\title{Convert a Vector to an `mchoice` Object}
\usage{
as.mchoice(x, levels, labels, label = NULL)
}
\arguments{
\item{x}{A vector to be converted into an `mchoice` object.}

\item{levels}{A vector of unique possible values (must match the length of `labels`).}

\item{labels}{A vector of labels corresponding to `levels` (must have the same length as `levels`).}

\item{label}{An optional character string providing a description of the multiple-choice field.}
}
\value{
An object of class `"mchoice"` with associated attributes:
\itemize{
  \item \code{levels} - The possible values for the multiple-choice variable.
  \item \code{labels} - Descriptive labels for each level.
  \item \code{label} - A descriptive name for the field (optional).
}
}
\description{
This function converts an existing vector into an `mchoice` object with predefined levels and labels.
}
\details{
The `as.mchoice()` function is useful when you need to convert an existing vector into an `mchoice` object.
}
\examples{
# Convert an existing vector to an mchoice object
responses <- c(1, 2, 1, 3)
m <- as.mchoice(
  x = responses,
  levels = c(1, 2, 3),
  labels = c("Yes", "No", "Maybe"),
  label = "Response Category"
)

# Check structure
str(m)

}
